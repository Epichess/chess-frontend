import React from 'react';
import PropTypes from 'prop-types';
import './Timer.scss';

const Timer = () => (
  <div className="Timer">
    Timer Component
  </div>
);

Timer.propTypes = {};

Timer.defaultProps = {};

export default Timer;
